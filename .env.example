# WireGuard interface name (default: wg0)
WG_INTERFACE=wg0

# WireGuard server port
WG_PORT=51820

# Client IP subnet base (without trailing dot, e.g. 10.0.0 → assigns 10.0.0.2, 10.0.0.3, ...)
WG_CLIENT_SUBNET=10.0.0

# DNS server for clients (usually the Pi-hole address)
WG_DNS=10.0.0.1
